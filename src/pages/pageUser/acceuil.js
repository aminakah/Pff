/* eslint-disable prettier/prettier */

import * as React from 'react';
import "../../App.css"



export default function AcceuilUser() {
 

    return (
      <div className="tabs">
        <h1
          className
          // ={selectedTab === "search" ? "tab-active" : ""
        // }
          // onClick={() => setSelectedTab("search")}
        >
          Recipe Search
        </h1>
        <h1
          // className=
          // {selectedTab === "favourites" ? "tab-active" : ""}
          // onClick={() => setSelectedTab("favourites")}
        >
          Favourites
        </h1>
      

      </div>

      
        
        
    );
}
